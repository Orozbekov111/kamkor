include: package:very_good_analysis/analysis_options.yaml

analyzer:
  exclude:
    - "**/*.g.dart"
    - "**/*.gr.dart"
    - "lib/core/localization/l10n/gen/**"

linter:
  rules:
    # App, not a published package — public API docs are not required.
    public_member_api_docs: false
